# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*

# Brochure extraction source (local only, PDFs stored externally)
brochures-source/

# `src/lib/projects.json` and `public/covers/` ARE committed so Vercel can
# build without running the extractor. Generate/regenerate them with
# `npm run extract -- --local` (PDFs stay out of git via brochures-source/).
